# OS / editor
.DS_Store
Thumbs.db
*.log
*.tmp

# WorkBuddy internal data (if present)
.workbuddy/

# Empty assets folder (no exported resources yet)
assets/

# Node server deps (installed by npm / Vercel, not committed)
server/node_modules/
